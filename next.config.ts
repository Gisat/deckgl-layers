import type { NextConfig } from "next";
import path from "path";

const nextConfig: NextConfig = {
  // webpack(config) {
  //   // Ignore specific files on the client (optional)
  //   config.module.rules.unshift({
  //     test: /\.(js|ts|tsx|mjs|cjs)$/,
  //     include: path.resolve(process.cwd(), "node_modules", "geoimage", "node_modules"),
  //     use: "ignore-loader",
  //   });

  //   return config;

  // }

};

export default nextConfig;