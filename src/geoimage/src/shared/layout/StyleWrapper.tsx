import React from 'react';
import {enumCombineValuesToString} from "@geoimage/shared/helpers/code.formating"
export const StylesWrapper = ({ children }) => {
    return (<div className="">{children}</div>)
}